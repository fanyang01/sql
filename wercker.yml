box: gcc

dev:
    steps:
        - script:
            name: Install libbsd-dev
            code: |
                apt-get update && apt-get install -y libbsd-dev

build:
  steps:
    - script:
        name: test
        code: |
          make testbuild
          make
